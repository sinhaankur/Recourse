{
  "name": "recourse-mcp",
  "version": "0.1.0",
  "description": "MCP server exposing Recourse's policy decoder and denial-letter reader as tools any MCP client (Claude Desktop, Claude Code) can call. Backed by local Ollama — no cloud, no API keys.",
  "license": "MIT",
  "author": "Ankur Sinha",
  "type": "module",
  "bin": {
    "recourse-mcp": "./dist/server.js"
  },
  "main": "./dist/server.js",
  "files": [
    "dist",
    "README.md"
  ],
  "scripts": {
    "build": "tsc",
    "dev": "tsc --watch",
    "start": "node dist/server.js"
  },
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.0.0",
    "zod": "^3.23.0"
  },
  "devDependencies": {
    "@types/node": "^22.0.0",
    "typescript": "~5.7.2"
  },
  "engines": {
    "node": ">=18"
  }
}
